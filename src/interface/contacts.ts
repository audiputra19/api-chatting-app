export interface Contact {
    firstName: string;
    lastName: string;
    phone: string;
}

export interface AllContact {
    firstName: string;
    lastName: string;
    phone: string;
    profile_image: string | null;
}